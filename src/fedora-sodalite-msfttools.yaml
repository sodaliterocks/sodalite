ref: fedora/35/${basearch}/sodalite-msfttools
repos:
  - copr-jose_exposito-touchegg
  - fedora-35
  - fedora-35-updates
  - microsoft
  - microsoft-edge
  - microsoft-vscode
packages:
  - code
  # BUG: dotnet won't install ("Running %post for dotnet-host: bwrap(/bin/sh): Child process killed by signal 1")
  #- dotnet-sdk-3.1 # .NET Core 3.1 (LTS 3-Dec-2022)
  #- dotnet-sdk-5.0 # .NET Core 5.0 (Current 8-May-2022)
  #- dotnet-sdk-6.0 # .NET Core 6.0 (LTS 8-Nov-2024)
  - microsoft-edge-stable
  # BUG: PowerShell won't install: attempts to install stuff into /usr/local/
  # SEE: https://github.com/projectatomic/rpm-ostree/issues/233
  #- powershell
#exclude-packages:
#  - firefox # This doesn't work. Why?
remove-from-packages: [
  ["firefox", "/usr/*" ]
]
include:
  - fedora-sodalite.yaml

rojig:
  name: fedora-sodalite-msfttools
  summary: "Sodalite"
  license: MIT
  description: "Sodalite with various Microsoft tools (.NET SDK, Edge, PowerShell, VSCode)"